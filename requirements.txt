# Versiones fijas, no rangos.
#
# Estaba en `streamlit>=1.40,<2`, que es una ruleta: cada reconstrucción del
# contenedor podía traer una versión distinta, y una versión mayor cambia APIs
# de las que depende el panel. Se fija y se sube a mano después de correr
# `python3 pruebas.py`.
#
# Verificado contra esta versión: st.plotly_chart(on_select), st.metric con
# chart_data, st.container(horizontal), st.dataframe(on_select) y
# column_config.ButtonColumn existen. st.echarts_chart NO existe en 1.60 —
# llega en 1.64— así que el panel no lo usa.
streamlit==1.60.0

# pandas por debajo de 3.0 a propósito: el salto de major cambia el manejo de
# copias y el dtype de texto por defecto, y eso no se ha probado contra este
# panel. Súbelo solo después de correr las pruebas.
pandas>=2.2,<3
numpy>=1.24,<3

# plotly por debajo de 7: el 7 eliminó TODAS las trazas `mapbox`
# (px.scatter_mapbox, go.Scattermapbox, layout.mapbox). Hoy el panel no las
# usa, pero fijarlo evita que alguien copie un tutorial viejo y reviente.
plotly>=6.0,<7

# Excel para la junta. Python puro, sin librerias del sistema: eso es lo que
# lo hace desplegable en Streamlit Cloud sin packages.txt. weasyprint, la
# alternativa obvia para PDF, exige Pango y por eso quedo descartada.
XlsxWriter>=3.2,<4

anthropic>=0.25.0
